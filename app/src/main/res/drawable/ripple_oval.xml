<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/green1"> <!-- Ripple color -->

    <!-- Ripple mask shape (optional but improves effect) -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="#ffffff" />
        </shape>
    </item>

    <!-- Background shape (your original oval) -->
    <item
        android:drawable="@drawable/bg_capture_button" />
</ripple>
